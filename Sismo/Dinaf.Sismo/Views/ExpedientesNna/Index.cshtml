@model List<Dinaf.Sismo.Application.ConsolidacionFamiliar.ExpedientesNna.DTOs.ExpedienteNnaDto>

<table id="expediente-nna-tbl" class="table datatable">
    <thead>
        <tr>
            <th scope="col">Numero de Expediente</th>
            <th scope="col">Nombre</th>
            <th scope="col">Genero</th>
            <th scope="col">Edad</th>
            <th scope="col">Fecha Creación</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var expediente in Model)
        {
            <tr>
                <td><a href="@Url.Action("Details", "ExpedientesNna", new { numeroExpediente = @expediente.NumeroExpediente })" onclick="showLoader()">@expediente.NumeroExpediente</a></td>
                <td>@expediente.DetalleNna.Nombre</td>
                <td>@(expediente.DetalleNna.Genero == "M" ? "Masculino" : "Femenino")</td>
                <td>@expediente.DetalleNna.Edad</td>
                <td>@expediente.FechaCreacion</td>
            </tr>
        }
    </tbody>
</table>